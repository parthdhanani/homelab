# Legacy artifacts

Historical material preserved for reference. Not used at runtime.

## `v2-rebuild/`
The original "v2" rebuild of the Cryptex stack (Jan–Feb 2026). This is what `/opt/cryptex/` evolved from. Kept here for diffability and to recover prior decisions if needed.

- `v2-rebuild-history.txt` — short git log of the original repo
- `v2-rebuild-history-detailed.txt` — log with per-commit stat

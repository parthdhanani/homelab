#!/usr/bin/env bash
# ╔══════════════════════════════════════════════════════════════════╗
# ║  Cryptex — Local Secrets (NEVER commit this file)              ║
# ║                                                                ║
# ║  Copy to secrets.env, fill in values, then:                    ║
# ║    source /opt/cryptex/secrets.env                             ║
# ║                                                                ║
# ║  This file is gitignored. Keep it only on the VPS.             ║
# ║  Backup: included in cryptex backup tarball via backup.sh      ║
# ╚══════════════════════════════════════════════════════════════════╝
set -a  # auto-export all variables

# ── Core ─────────────────────────────────────────────────────────
DOMAIN=""
CF_TUNNEL_TOKEN=""

# ── PostgreSQL ────────────────────────────────────────────────────
POSTGRES_USER="admin"
POSTGRES_PASSWORD=""

MOODLE_DB_USER="moodle_user"
MOODLE_DB_PASSWORD=""

N8N_DB_USER="n8n_user"
N8N_DB_PASSWORD=""

TRAXLRS_DB_USER="traxlrs_user"
TRAXLRS_DB_PASSWORD=""

FORGEJO_DB_USER="forgejo_user"
FORGEJO_DB_PASSWORD=""

MINIFLUX_DB_USER="miniflux_user"
MINIFLUX_DB_PASSWORD=""

UMAMI_DB_USER="umami_user"
UMAMI_DB_PASSWORD=""

SHLINK_DB_USER="shlink_user"
SHLINK_DB_PASSWORD=""

LIBRECHAT_DB_USER="librechat_user"
LIBRECHAT_DB_PASSWORD=""

FERRETDB_DB_USER="ferretdb_user"
FERRETDB_DB_PASSWORD=""

OB1_DB_PASSWORD=""

# ── Redis ─────────────────────────────────────────────────────────
REDIS_PASSWORD=""

# ── Services ──────────────────────────────────────────────────────
MOODLE_ADMIN_USER="admin"
MOODLE_ADMIN_PASSWORD=""
MOODLE_ADMIN_EMAIL=""
MOODLE_SITE_NAME="Moodle"

TRAXLRS_APP_KEY=""                      # base64:... — run: php artisan key:generate --show
TRAXLRS_ADMIN_EMAIL=""
TRAXLRS_ADMIN_PASSWORD=""
TRAXLRS_ENDPOINT_USERNAME="traxlrs"
TRAXLRS_ENDPOINT_PASSWORD=""

VAULTWARDEN_ADMIN_TOKEN=""              # argon2id hash — generate with: ./scripts/gen-vaultwarden-token.sh
VAULTWARDEN_SIGNUPS_ALLOWED="false"
VAULTWARDEN_SSO_ENABLED="true"

FORGEJO_SECRET_KEY=""                   # 64 random hex: openssl rand -hex 32
FORGEJO_DISABLE_REGISTRATION="true"

N8N_ADMIN_PASSWORD=""
N8N_ENCRYPTION_KEY=""                   # openssl rand -hex 16

KOPIA_SERVER_USER="admin@homelab"
KOPIA_SERVER_PASSWORD=""
KOPIA_PASSWORD=""                       # repository encryption password

SHLINK_API_KEY=""
SHLINK_DOMAIN="${DOMAIN}"

UMAMI_APP_SECRET=""

MINIFLUX_ADMIN_USER="admin"
MINIFLUX_ADMIN_PASSWORD=""

SEARXNG_SECRET_KEY=""

# ── PocketID (OIDC) ──────────────────────────────────────────────
POCKETID_ENCRYPTION_KEY=""
POCKETID_VAULTWARDEN_CLIENT_ID=""
POCKETID_VAULTWARDEN_CLIENT_SECRET=""
POCKETID_MINIFLUX_CLIENT_ID=""
POCKETID_MINIFLUX_CLIENT_SECRET=""
POCKETID_LIBRECHAT_CLIENT_ID=""
POCKETID_LIBRECHAT_CLIENT_SECRET=""
POCKETID_LIBRECHAT_SESSION_SECRET=""

# ── LibreChat ─────────────────────────────────────────────────────
LIBRECHAT_JWT_SECRET=""
LIBRECHAT_JWT_REFRESH_SECRET=""
LIBRECHAT_CREDS_KEY=""                  # openssl rand -hex 32
LIBRECHAT_CREDS_IV=""                   # openssl rand -hex 16
LIBRECHAT_ALLOW_SOCIAL_LOGIN="true"

# ── AI API Keys ───────────────────────────────────────────────────
GEMINI_API_KEY=""
OPENROUTER_KEY=""
GROQ_API_KEY=""
NVIDIA_NIM_API_KEY=""
ANTHROPIC_API_KEY=""

# ── SMTP ──────────────────────────────────────────────────────────
SMTP_USER=""
SMTP_PASSWORD=""

# ── Alerts ────────────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""
UPTIME_KUMA_BACKUP_PUSH_URL=""
UPTIME_KUMA_VERIFY_PUSH_URL=""

# ── Banking (n8n) ────────────────────────────────────────────────
BANK_EMAIL_DOMAIN=""
BANK_PDF_PASSWORD=""

# ── Deployment ────────────────────────────────────────────────────
DEPLOY_PROFILE="full"
DEPLOY_SERVICES=""

set +a
